services:
  onboard:
    build: .
    container_name: onboard
    devices:
      - "/dev/video4:/dev/video4"
      - "/dev/serial0:/dev/serial0"
    network_mode: host        
    volumes:
      - ./src/streamer.cfg:/root/streamer.cfg:ro
